<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container">
    <%= link_to "BRAND", root_path, class: "navbar-brand fw-bold" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse align-items-center" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <%= link_to "Home", root_path, class: "nav-link" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to "New Event", new_event_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
      <hr class="d-lg-none">
      <% if user_signed_in? %>
        <div class="ms-auto hstack gap-3">
          <div class="hstack h6 my-0" >
            <i class="bi bi-person-circle me-2 my-0"></i>
            <p class="my-0"><%= current_user.first_name %> <%= current_user.last_name %></p>
          </div>
          <div class="vr d-lg-none"></div>
          <%= button_to "Logout", destroy_user_session_path, method: :delete ,class: "btn btn-outline-dark" %>
        </div>
      <% else %>
        <div class="ms-auto hstack gap-3">
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline-dark" %>
          <div class="vr d-lg-none"></div>
          <%= link_to "Register", new_user_registration_path, class: "btn btn-dark" %>
        </div>
      <% end %>
    </div>
  </div>
</nav>